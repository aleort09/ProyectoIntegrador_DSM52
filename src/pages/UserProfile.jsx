import React from "react";
import UserDetails from "../components/UserDetails";

const UserProfile = () => {
    return (
        <div>
            <UserDetails />
        </div>
    );
};

export default UserProfile;